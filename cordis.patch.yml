# dsh-restart bundle patch
#
# Mounts the host half (the /dsh-restart/* HTTP routes) into the web profile.
# The browser half is discovered automatically through the `dsh.client`
# declaration in package.json.
- insert:
    - id: restart
      name: 'dsh-restart'
